Sintassi accettata delle parentesi graffe:
    parentesi graffa aperta: accettata solo sulla stessa riga delle parole chiave (server, location ecc)
    parentesi graffa chiusa: accettata solo in una riga dove é presente solo la stessa parentesi

Max client body size:
	va espressa in un numero puro che indica i bytes

To-do:
    il nome di un file potrebbe contenere le parentesi graffe, aggiungere controllo che non faccia tenere in conto delle parentesi graffe dopo l escape code '\'
